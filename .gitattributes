# 强制 shell 脚本使用 LF，避免 Windows CRLF 导致 Linux 上
# "bad interpreter: /usr/bin/env bash^M" 错误
*.sh text eol=lf

# 其它文本文件
*.md   text eol=lf
*.kdl  text eol=lf
*.toml text eol=lf

# 二进制
*.png binary
*.jpg binary
